getDrawPeriod.t.sol
├── when caller is not the owner
│   └── it should revert
└── when caller is the owner
    ├── when the new luck factor list is empty
    │   ├── it should revert with {DRAW_UPDATE_LUCK_FACTOR__INVALID_LUCK_FACTOR_LIST} error
    └── when the new luck factor list is not empty
        ├── it should update the luck factor list
        └── it should emit the {LuckFactorUpdated} event
